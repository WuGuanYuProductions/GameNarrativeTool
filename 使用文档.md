// Produced By Wu Guanyu Productions, All Rights Reserved.


**故事线蓝图编辑器使用说明文档**

**版本：** 1.0
**目标受众：** 游戏剧情设计师、文案策划、关卡设计师

---

### 1. 简介
本软件是一款专为游戏叙事和剧情设计师打造的节点化故事线蓝图编辑器。设计师可以通过可视化的节点连线快速搭建复杂的网状叙事逻辑、分支对话以及演出转场控制。软件内置了极其方便的“剧本表格”与“节点蓝图”双向转换功能，并提供完善的多语言本地化支持和实时的剧情运行模拟沙盒，最终产物可直接导出为数据表格或JSON供游戏引擎读取。

---

### 2. 界面布局概览
启动软件后，系统分为两个核心界面：**故事管理列表（主界面）** 与 **蓝图编辑工作台**。

#### 2.1 故事管理列表
*   **左侧列表区：** 显示当前工程内所有的故事线文件，并标识最近打开的项目。
*   **右侧操作区：** 包含界面语言切换（中/英）、新建、打开、重命名、另存为、导出表格、进入剧本编辑器以及删除故事线的全局操作按钮。

#### 2.2 蓝图编辑工作台
双击故事列表中的任意项目即可进入蓝图工作台，界面划分为以下区域：
*   **顶部工具栏：** 包含返回列表、运行模式切换（自动/手动）、播放语言选择、运行/结束控制、回到故事开始节点、本地化管理以及保存和导出操作。
*   **中央蓝图画布：** 无限缩放的网格工作区，用于创建、连接和排布各类剧情逻辑节点。
*   **左下方演出预览栏：** 在模拟运行剧情时，此处将以剧本对话的格式实时打印当前运行到的节点内容，并提供分支选项的点击交互。
*   **右侧属性面板：** 选中画布中的任意节点后，此处将展示该节点的所有详细参数，支持实时修改并提供多语言文本的录入支持。

---

### 3. 基础项目与本地化管理

#### 3.1 故事线的创建与保存
*   **新建：** 在主界面点击“新建故事线”，输入名称即可创建空白蓝图。
*   **保存与退出：** 在蓝图工作台中，点击“保存故事线”可保存当前修改。若在未保存状态下点击“返回故事列表”或关闭软件，系统会弹出确认提示，防止数据丢失。

#### 3.2 管理多语言 (本地化)
为了适应游戏的全球化发行，软件内置了多语言管理：
1.  在蓝图工作台顶部点击“管理本地化”按钮。
2.  在弹出的对话框中，输入需要增加的语言代号（如 English、JP），点击确认添加。
3.  添加后，在属性面板的文本输入框上方会出现“选择语言”下拉菜单，可以针对不同语言录入对应的对话或选项台本。

---

### 4. 节点与蓝图基础操作

#### 4.1 节点的创建与选择
*   **新建节点：** 在中央画布的空白处单击鼠标右键，在弹出的菜单中选择需要的节点类型。
*   **选择节点：** 鼠标左键点击任意节点即可选中。选中的节点边框会高亮显示为金色。按住鼠标左键在空白处拖拽可框选多个节点。

#### 4.2 连线与逻辑流
*   **建立连接：** 每个节点左右两侧有圆形端口。左侧为输入端口，右侧为输出端口。鼠标左键按住输出端口并拖拽，拉出连接线，将其连接到下一个节点的输入端口上。
*   **防止死循环：** 系统内置回环检测，如果您的连线会导致剧情逻辑进入死循环，系统将拒绝该连线操作并弹出警告。
*   **删除连线：** 选中一条连线，按下 `Delete` 键，或右键选择“删除连接线”。也可按住 `Ctrl` 键并点击端口，快速清除该端口上的所有连线。

#### 4.3 视图导航
*   **移动画布：** 按住鼠标中键并拖拽，可以平移当前视野。
*   **缩放画布：** 滚动鼠标滚轮可对画布进行放大和缩小。
*   **视角复位：** 点击顶部“回到故事开始”按钮，画面将瞬间居中对齐到“故事开始”节点。

---

### 5. 节点类型与属性面板详解
每种节点承载不同的剧情功能，选中后可在右侧面板设置其参数：

*   **故事开始 (Start)：**
    *   每个蓝图必须且只能包含一个此节点，它是剧情运行和导出的唯一入口。
*   **对话节点 (Dialogue)：**
    *   **核心字段：** 场次号、对话ID、说话人、情绪、景别、场景描述、对话台本（支持多语言）。
    *   **注释：** 额外的策划备注。输入的内容将以悬浮气泡的形式显示在蓝图画布中的节点上方，通过“注释透明度”滑块可调节气泡的显示效果。
    *   **播放时长：** 自动运行模式下，该对话停留的秒数。
*   **对话分支节点 (Branch)：**
    *   用于构建玩家选项。点击面板中的“添加分支 (+)”可新增选项。
    *   每个选项均支持独立配置选项文本（支持多语言）、关联的场次号、对话ID和注释。
    *   随着选项的增加，节点右侧会自动生成对应的输出端口，用于连接不同分支的后续剧情。
*   **场景提示 (Scene Prompt)：**
    *   用于输出旁白或无说话人的场景动作描述。
*   **备注节点 (Note)：**
    *   仅用于在蓝图画布上做视觉上的逻辑区块说明，不会影响实际的游戏流程导出。可修改字体颜色以便于视觉区分。
*   **转场 (Transition)：**
    *   定义剧情画面之间的过渡效果。可设置“是否淡入淡出”以及过渡的时长。
*   **跳转起点 / 跳转终点 (Jump Start / Jump End)：**
    *   当蓝图极为庞大时，直接连线会导致画布混乱。
    *   通过设置相同的“跳转标识”（如“剧情点A”），可以将“跳转起点”和“跳转终点”在逻辑上无缝连接，无需实际拉线。

---

### 6. 剧情运行与调试功能
软件内置了实时模拟器，用于在交付引擎前验证剧情逻辑的连贯性。

#### 6.1 运行模式
*   **自动运行：** 根据节点设定的“播放时长”，系统会自动按顺序推进剧情。
*   **手动运行：** 每推进一个节点，系统会等待操作，点击左下角“演出实时预览栏”内的任意位置手动推进下一步。

#### 6.2 实时预览与本地化测试
*   在顶部工具栏选择“播放语言”（默认为默认语言，也可以选择您添加的英语等其他语言）。
*   点击“▶ 开始运行”，剧情将在左下方预览栏中以剧本格式逐行输出。
*   如果运行到“对话分支节点”，预览栏会暂停并显示可点击的按钮，点击对应按钮即可进入相应的剧情分支。
*   **本地化缺失警告：** 运行时若系统检测到当前设定的播放语言在某个节点未配置文本，将输出黄色警告并自动停止运行。

#### 6.3 错误校验与断点调试
*   **语法校验：** 点击运行时，系统会自动全盘扫描。如果存在“对话ID为空”、“播放时长为0”、或者“跳转标识找不到终点”等情况，问题节点将整块飘红，并弹出错误列表提示修改。
*   **查重功能：** 如果系统检测到存在“场次号”与“对话ID”完全一致的重复节点，将立即弹窗警告，并将重复的节点标红。
*   **断点 (Breakpoint)：** 右键点击某个节点选择“切换断点” (或选中后按 `Ctrl+T`)。节点右上角将出现红色圆点。自动运行模式下，到达该节点时将强制暂停，方便排查分支流向。

---

### 7. 剧本编辑器 (高级双向工作流)
对于习惯使用Excel的文案策划，软件提供了类似电子表格的极速录入界面。在主界面点击“剧本编辑器”进入。

#### 7.1 表格操作
*   该界面为标准的行列分布。通过顶部“添加行”和“删除行”管理数据。
*   第一列为“功能类型”（对话节点、分支选项、转场等），后续列为场次号、说话人及各类本地化台本列。
*   支持从外部Excel/CSV批量复制内容，然后直接在表格内按 `Ctrl+V` 粘贴，系统会自动向下扩展并填入数据。
*   **实时查重：** 在表格内修改场次号和对话ID时，如果有重复，对应行将立刻变为暗红色背景。

#### 7.2 表格与蓝图的双向转换
*   **从蓝图导入：** 点击顶部按钮，可选择现有蓝图文件，系统会自动根据节点连线的顺序，将其展平为表格内容，方便集中校对错别字。
*   **一键转换为蓝图大纲：** 在表格录入完毕后，点击该按钮，系统会自动将表格里的所有行按顺序转换为可视化节点，并自动完成连线和分支绑定，生成一个全新的蓝图故事线，极大地提升了白盒阶段的产出效率。
*   **导入/导出：** 表格支持导出为单独的 `.xlsx` 或 `.csv` 格式进行外部传阅。

---

### 8. 数据导出
完成编辑后，设计数据需要导出以供程序及游戏引擎读取。
1.  **导出为故事线 (JSON)：**
    保存所有的节点坐标、连线逻辑和属性，生成纯代码格式的文件，通常直接作为游戏引擎的剧情配置文件解析。
2.  **导出为表格 (Excel/CSV)：**
    系统会按照节点的连线遍历顺序，将整个树状图结构“压平”并导出为标准的对话总表。包含完整的中文及已添加的本地化语言列，可直接提交给音频部门用于配音录制参考。
    *注：导出为 xlsx 格式需要本地 Python 环境安装有 pandas 和 openpyxl 库，否则系统将提示并建议降级导出为 csv 格式。*

---

### 9. 快捷键列表
为了提升编辑效率，蓝图工作台支持以下快捷操作：
*   **撤销：** `Ctrl + R` (撤销上一步的节点变动或连线)
*   **复制选中节点：** `Ctrl + C`
*   **剪切选中节点：** `Ctrl + X`
*   **粘贴节点：** `Ctrl + V` (将粘贴在鼠标当前悬停的位置)
*   **原地快速复制：** `Ctrl + D` (将选中的节点复制并在其右下方快速生成)
*   **删除选中项：** `Delete`
*   **切换断点：** `Ctrl + T`
*   **快速清理端口连线：** 连线状态下，按住 `Ctrl` 键并点击节点输入/输出端口
